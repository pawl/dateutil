* add "MonkeyType" to requirements-dev.txt
* replace "python -m pytest" in tox.ini with "monkeytype run -m pytest"
    [testenv]
    commands = monkeytype run -m pytest {posargs: "{toxinidir}/dateutil/test" "{toxinidir}/docs" --cov-config="{toxinidir}/tox.ini" --cov=dateutil}
* mkdir stubs
* monkeytype list-modules | xargs -n1 -I{} sh -c 'monkeytype stub {} > stubs/{}.pyi'
* run move_files.py
* add mypy task to tox.ini
* start running mypy with "tox -e mypy"
* remove duplicate imports added by merge-pyi, fix import sorting issues
* clean up super long type hints by inlining with params
